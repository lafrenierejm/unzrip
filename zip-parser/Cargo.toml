[package]
name = "zip-parser"
description = "Parse Zip contents"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thiserror = "2"
memchr = "2"
memutils = { path = "../memutils" }
